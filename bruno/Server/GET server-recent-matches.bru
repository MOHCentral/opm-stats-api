meta {
  name: Server Recent Matches
  type: http
  seq: 1
}

get {
  url: {{base_url}}/servers/{{var:id}}/matches
  body: none
  auth: none
}

params:query {
  ~limit: 20
}

docs {
  # Server Recent Matches
  
  ## Responses
  
  - **200**: Matches
  - **500**: Internal Error
}

tests {
  test("Status code is 2xx", function() {
    expect(res.status).to.be.within(200, 299);
  });
}
