meta {
  name: Init Device Auth
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/device
  body: none
  auth: none
}

docs {
  # Init Device Auth
  
  ## Responses
  
  - **200**: Token Info
  - **400**: Bad Request
}

tests {
  test("Status code is 2xx", function() {
    expect(res.status).to.be.within(200, 299);
  });
}
