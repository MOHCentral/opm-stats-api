meta {
  name: Ingest Match Result
  type: http
  seq: 1
}

post {
  url: {{base_url}}/ingest/match-result
  body: none
  auth: none
}

headers {
  X-Server-Token: {{var:server_token}}
}

docs {
  # Ingest Match Result
  
  ## Responses
  
  - **200**: Processed
  - **400**: Bad Request
}

tests {
  test("Status code is 2xx", function() {
    expect(res.status).to.be.within(200, 299);
  });
}
